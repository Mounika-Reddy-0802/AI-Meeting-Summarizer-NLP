# Placeholder — the real compose file is Mounika's Week 3 task
# (PROJECT_PLAN.md §7 "week3-mounika-docker-release"): backend + frontend services,
# a volume for the downloaded model checkpoint, and env passthrough from .env.
#
# Until then, run the two services in separate terminals as described in
# backend/README.md and frontend/README.md.

services: {}
