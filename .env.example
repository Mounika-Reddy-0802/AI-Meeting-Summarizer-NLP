# Root environment template - names only, never real values.
# Copy to .env for docker compose. Per-service templates live in backend/.env.example
# and frontend/.env.example.

# --- backend ---
JWT_SECRET=
DATABASE_URL=
MODEL_DIR=
WHISPER_SIZE=

# --- frontend ---
NEXT_PUBLIC_API_URL=
